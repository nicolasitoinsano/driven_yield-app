name: driven_yield
description: Aplicación Flutter basada en los mockups de Driven Yield Citas.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  googleapis: ^13.1.0
  googleapis_auth: ^1.6.0
  http: ^1.2.2
  intl: ^0.20.2
  supabase_flutter: ^2.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
